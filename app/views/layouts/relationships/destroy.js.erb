//jQuery("#follow_form").update("<%= escape_javascript(render('users/follow')) %>");
//jQuery('#followers').update('<%= "#{pluralize(@user.followers.count, 'follower')}" %>');
$( function() {
  $('title').text("Destroy")
})